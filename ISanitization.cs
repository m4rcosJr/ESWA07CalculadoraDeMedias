namespace OOPFoundation
{
    /// <summary>
    /// Interface para sanitização de texto.
    /// </summary>
    public interface ISanitization
    {
        string Sanitize(string textToSanitize);
    }
}
